# Risk register

Last updated: 2026-07-28 (Asia/Kolkata)

Probability and impact use Low / Medium / High / Critical. “Owner” names the
accountable function, not an individual commitment.

| ID | Description | Probability | Impact | Mitigation / required evidence | Owner | Status |
|---|---|---:|---:|---|---|---|
| R-001 | Redacted Gitleaks history scanning classified seven records: four public Supabase client configurations and three private/server credentials. Private credential revocation remains unconfirmed. Native GitHub secret scanning is enabled and a redacted PR scanner is implemented. | High | Critical | Credential owners must complete the provider-side actions in `docs/security/HISTORICAL_CREDENTIAL_RESPONSE.md`; keep native and CI scans enabled; do not rewrite history before revocation and coordination. | Security / Repository owner | OPEN — revocation confirmation blocker |
| R-002 | Next.js AI routes accept unauthenticated requests and client-supplied opportunity content; Edge Functions contain no explicit auth/quota/size controls. This enables quota abuse and shared-cache poisoning. | High | Critical | M3: authenticate, load canonical records server-side, validate payloads, limit size/time/rate, log usage, and test cache isolation. | Security / Backend | OPEN — launch blocker |
| R-003 | Supabase cannot be recreated from Git: no `config.toml`, migration directory, seed contract, or ordered migration history. Four literal code-referenced tables are absent from SQL. | Certain | Critical | M2: inventory production, back up, create additive ordered migrations, replay on a fresh local project, verify schema and rollback. | Database | OPEN — launch blocker |
| R-004 | Private records use email ownership rather than `auth.users.id`; no two-user RLS isolation test exists. A legacy mobile anon key is embedded as expected for a public client, increasing reliance on correct RLS. | High | Critical | M2/M3: migrate ownership to UUID, add foreign keys, test User A/User B isolation, review all exposed schemas and Storage policies. | Database / Security | OPEN — launch blocker |
| R-005 | Ingestion and admin paths permanently delete expired opportunities/corrigendums, and the admin cleanup endpoint includes an irreversible purge mode. Historical opportunity data can be lost. | High | Critical | M1: additive lifecycle states/history, disable purge by default, verified backup/restore, archive transition tests. | Ingestion / Database | OPEN — launch blocker |
| R-006 | Scheduled ingestion does not pass `--strict-health`; the observed recent run set contains 5 successes, 5 failures, 7 cancellations, and 1 in progress. Current persistence is not source-checkpointed. | High | Critical | M1: tune and enable strict health, record runs/failures, checkpoint per source, alert on freshness, prove partial recovery and idempotency. | Ingestion / DevOps | OPEN — launch blocker |
| R-007 | M0 found no usable ESLint, a TypeScript error, skipped build type validation, and no primary frontend/Python PR workflow. M0.1 repairs pass locally and remotely; `main` now requires the four verified launch checks. | Low | High | Keep `Frontend gates`, `Python gates`, `Secret scan`, and `Audit tooling` required and current; review any rule/config change. | Frontend / QA | RESOLVED FOR CURRENT GATE SET |
| R-008 | M0 reported 5 high and 2 moderate production npm findings. M0.1 patched Next.js, PostCSS, shadcn, and the affected transitive chains; `npm audit --omit=dev --audit-level=high` now reports zero vulnerabilities. | Low | High | Keep reviewed package overrides and both lockfiles current; retain the production audit as a required check. | Frontend / Security | RESOLVED FOR CURRENT PRODUCTION GRAPH |
| R-009 | Next.js labels global `ai_score` rankings as “Recommended for you/business” and “AI Match Score” without loading the authenticated profile. | Certain | High | Immediately use honest global-score labels, then implement separate versioned contractor/job-seeker engines in M5. | Product / Backend | OPEN — truthfulness blocker |
| R-010 | Admin allowlist falls back to a hardcoded founder email; admin actions have no audit log. | High | High | M3: fail closed when admin config is absent, use a durable admin role, add audit events and tests. | Security / Admin | OPEN — launch blocker |
| R-011 | Published opportunity records have partial provenance but no complete verification workflow, evidence model, correction audit, or source-tier enforcement. | High | High | M4 verification states, official URL checks, field evidence, review UI, user reports, and sampled quality thresholds. | Data / Product | OPEN |
| R-012 | `source_health`, `saved_jobs`, `payments`, and `user_profiles` are referenced literally but not created by version-controlled SQL. The `vault` bucket is manual dashboard setup. | Certain | High | M2 migrations and schema verification; resolve `profiles` vs `user_profiles`; version Storage bucket/policies. | Database | OPEN |
| R-013 | India VM setup defaults to a stale feature branch; standalone VM/Windows timers can run outside GitHub Actions concurrency and create duplicate/racing ingestion. | Medium | High | Pin reviewed `main`/release SHA, choose one scheduler authority, add runner identity/run locks and documented rollback. | DevOps | OPEN |
| R-014 | Python dependencies are lower-bounded rather than pinned and have no lock/hash file. A fresh install can change behaviour despite the same commit. | High | Medium | Add a reviewed lock/constraints strategy and repeatable browser/runtime versions. | Python / DevOps | OPEN |
| R-015 | There is no complete provider-neutral production observability, incident runbook, backup/restore rehearsal, or rollback evidence. | High | Critical | M7 interfaces, actionable alerts, backup/storage procedures, restore rehearsal, deployment rollback, incident ownership. | DevOps / Database | OPEN — launch blocker |
| R-016 | Vercel reports a successful production deployment for the baseline commit and the canonical public URL returns HTTP 200, but authenticated, data, AI, admin, and rollback behaviour was not production-validated. | Medium | High | Controlled production smoke suite with non-destructive test accounts and evidence. | QA / DevOps | OPEN |
| R-017 | Payment code lacks complete amount/order reconciliation, event idempotency log, entitlement expiry/refund/cancellation handling, and test-mode enforcement. Configuration state in production is unknown. | Medium | Critical | Keep disabled; M8 test-mode-only implementation and explicit human activation gate. | Payments / Security | OPEN — activation blocker |
| R-018 | No explicit Content Security Policy or complete security-header configuration exists in Next.js config. | High | Medium | M3 header/CSP design, deploy in report-only where needed, test OAuth/Supabase/AI/payment integrations. | Frontend / Security | OPEN |
| R-019 | `alert_log` has an allow-all RLS policy; discovery/admin reads use the anon Supabase client while discovery tables have no anon policy, producing either overexposure or a non-functional admin UI depending on grants. | High | High | M2/M3 least-privilege policies and server-only admin data path; test actual grants plus RLS. | Database / Security | OPEN |
| R-020 | Environment examples omit multiple used variables (admin, payments, workflow tuning), while comments/defaults differ across scrapers. | High | Medium | Use `environment-variables.md` as baseline; add validated templates/schema in the owning milestone. | DevOps | OPEN |
