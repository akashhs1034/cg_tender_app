# Opporta requirements.txt
# Install: python -m pip install -r requirements.txt
# After install, also run: python -m playwright install chromium

# Frontend
streamlit>=1.40

# Data
pandas>=2.2
python-dateutil>=2.9      # core.py: from dateutil import parser

# Database
supabase>=2.7

# Scraping
playwright>=1.45
beautifulsoup4>=4.12
requests>=2.32

# Charts
plotly>=5.20

# AI evaluator + bid generation
google-genai>=2.0         # Gemini API (preferred — set GEMINI_API_KEY in .env)
anthropic>=0.40           # Claude fallback (set ANTHROPIC_API_KEY in .env)
pdfplumber>=0.11          # app.py uses pdfplumber.open() — not pypdf
pillow>=10.0              # logo for st.set_page_config page_icon
python-docx>=1.0          # bid_engine.py: build Word bid documents

# Email alerts
resend>=2.0

# Local dev secrets (.env file)
python-dotenv>=1.0
