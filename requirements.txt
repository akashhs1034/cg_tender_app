streamlit>=1.40
pandas>=2.2
python-dotenv>=1.0
python-dateutil>=2.9
supabase>=2.7
requests>=2.32
beautifulsoup4>=4.12
pdfplumber>=0.11          # read uploaded contractor documents
# Optional — real-AI extraction in the evaluator (set ANTHROPIC_API_KEY):
# anthropic>=0.40
# JS-heavy portal scrapers (run `playwright install chromium` after pip install):
playwright>=1.45
